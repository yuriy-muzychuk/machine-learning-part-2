# Індивідуальний проєкт
## Вибір датасету

Для цього проєкту кожен студент обирає **власний датасет** з Kaggle.

Щоб зареєструвати свій вибір, перейдіть за посиланням і заповніть форму:
**[Підписатись на датасет](https://lnueduua-my.sharepoint.com/:x:/g/personal/yuriy_muzychuk_lnu_edu_ua/IQCl5yb-mlmASan4VOAWhmidAXPaPzSUa3vEW6pb3JrLaQ4?e=PDzc9a)**

**Вимоги до датасету:**

- Джерело: Kaggle (табличні дані, формат CSV).
- Задача: регресія або бінарна / багатокласова класифікація.
- Обсяг: не менше 1 000 і не більше 500 000 рядків.
- Ознаки: мінімум 8 ознак, бажано суміш числових та категоріальних.
- Цільова змінна: чітко визначена; не є ознакою ідентифікатора чи індексу.
- Датасет не повинен збігатися з тим, що використовувався у лабораторних
  роботах або домашніх завданнях курсу.

> **Один датасет - один студент.** Після реєстрації датасет закріплюється
> за вами. Перевірте форму перед вибором, щоб уникнути дублювання.

У першій клітинці ноутбука вкажіть:

```python
# Dataset: <назва датасету>
# URL:     <посилання на Kaggle>
# Task:    classification | regression
# Target:  <назва цільової колонки>
```

---

## Структура проєкту

Проєкт виконується у **двох Jupyter-ноутбуках**:

| Ноутбук | Зміст |
|---|---|
| `capstone_part1_eda_pipeline.ipynb` | Частини 1-4: EDA, попередня обробка, ознаки, візуалізація |
| `capstone_part2_model.ipynb` | Частини 5-8: Навчання, оцінка, оптимізація, навчання представлень |

---

## Частина 1. Завантаження та розвідувальний аналіз (EDA)

### 1.1 Завантаження та огляд

- Завантажте датасет та помістіть його у DataFrame.
- Виведіть розміри, типи колонок та загальну статистику (`describe()`).
- Визначте числові та категоріальні ознаки. Скільки їх?

### 1.2 Цільова змінна

**Класифікація:**
- Відобразіть розподіл класів у вигляді стовпчастої діаграми з відсотками.
- Чи є дисбаланс класів? Як це вплине на вибір метрики оцінки?
- Обґрунтуйте, чому `accuracy` може бути оманливою метрикою за наявності
  дисбалансу.

**Регресія:**
- Відобразіть розподіл цільової змінної (гістограма + KDE).
- Чи потребує вона логарифмічного перетворення? Перевірте візуально та за коефіцієнтом асиметрії (skewness).

### 1.3 Числові ознаки

- Побудуйте гістограми для всіх числових ознак.
- Визначте ознаки з викидами (IQR або візуально). Чи мають вони сенс у предметній області?
- Побудуйте кореляційну матрицю. Які пари ознак найбільш корельовані між собою? Які найбільш корельовані з цільовою змінною?

### 1.4 Категоріальні ознаки (якщо є)

- Відобразіть частоту категорій для кожної категоріальної ознаки.
- Для 3-4 найважливіших ознак побудуйте grouped bar plot (класифікація) або boxplot (регресія), що показує зв'язок з цільовою змінною.

### 1.5 Пропущені значення

- Перевірте датасет на наявність явних пропусків (`NaN`) та прихованих
  (рядкові замінники типу `'unknown'`, `'N/A'`, `-1`, `999` тощо).
- Відобразіть частку пропусків для кожної колонки.
- Запропонуйте стратегію обробки для кожного типу пропусків
  та обґрунтуйте вибір.

---

## Частина 2. Розбиття даних та попередня обробка

### 2.1 Розбиття

- Виконайте розбиття: train / validation / test у
  пропорції **70 / 15 / 15**, `random_state=42`.
  - Класифікація: стратифікуйте за цільовою змінною.
  - Регресія: стратифікація не обов'язкова; перевірте розподіл цілі у кожній частині.
- Перевірте розподіл цільової змінної у кожній частині.

> **Важливо:** тестова вибірка використовується **лише один раз** - для
> фінальної звітності. Вся підбірка гіперпараметрів виконується на train + val.

### 2.2 Попередня обробка

Побудуйте `sklearn`-сумісний пайплайн (`ColumnTransformer` + `Pipeline`):

**Числова гілка:**
- Власний трансформер для обрізання викидів (clip за 1-м і 99-м перцентилем).
- `SimpleImputer(strategy='median')`
- `StandardScaler`

**Категоріальна гілка (якщо є):**
- Замініть відомі рядкові замінники на `np.nan` перед пайплайном.
- `SimpleImputer(strategy='most_frequent')`
- `OneHotEncoder(handle_unknown='ignore', sparse_output=False)`

- Натренуйте пайплайн **тільки на train**. Застосуйте трансформацію до train, val та test.
- Виведіть фінальну кількість ознак після кодування.

---

## Частина 3. Інженерія та відбір ознак

### 3.1 Нові ознаки

Створіть **мінімум 3** нові ознаки до застосування пайплайну.

Для кожної нової ознаки:
- Опишіть, як вона обчислюється.
- Поясніть її предметний сенс.
- Наведіть попередню оцінку її значущості (кореляція з ціллю або різниця в розподілах між класами).

### 3.2 Відбір ознак

- Після кодування обчисліть значущість кожної ознаки відносно цільової змінної.
- Відобразіть топ-20 ознак за значущістю у вигляді горизонтальної стовпчастої діаграми.
- Зробіть висновок: які групи ознак домінують і що це говорить про задачу?

---

## Частина 4. Візуалізація у зниженому просторі (PCA та t-SNE)

> Для PCA i t-SNE використовуйте **підвибірку** не більше 3 000 зразків тренувального набору.

### 4.1 PCA

- Застосуйте **власну реалізацію PCA** до нормалізованих даних.
- Побудуйте графік поясненої дисперсії (scree plot): яка кількість компонент пояснює 90% дисперсії?
- Відобразіть проєкцію на перші 2 компоненти:
  - Класифікація: розфарбуйте точки за класом.
  - Регресія: розфарбуйте точки за значенням цілі за допомогою
    неперервної палітри (наприклад, `plt.cm.viridis`).
- Відобразіть навантаження (loadings) першого компоненту: які вихідні ознаки вносять найбільший вклад?

### 4.2 t-SNE

- Застосуйте **власну реалізацію t-SNE** (з лабораторної роботи) до підвибірки.
- Спробуйте значення `perplexity` ∈ {15, 30, 50}. Відобразіть три проєкції поряд з однаковою схемою розфарбування, що й у PCA.
- Порівняйте t-SNE та PCA проєкції: яка з них краще виявляє структуру  даних? Чи видно чіткі кластери?

---

## Частина 5. Базова модель (Baseline)

Перш ніж навчати нейронну мережу, встановіть базовий рівень якості.

### 5.1 Наївний класифікатор / регресор

- Класифікація: `DummyClassifier(strategy='most_frequent')`.
- Регресія: `DummyRegressor(strategy='mean')`.
- Виведіть базові метрики на **validation** наборі.
- Поясніть, чому ці метрики виглядають так, як виглядають.

### 5.2 Лінійна модель

- Класифікація: `LogisticRegression(max_iter=1000, random_state=42)`.
- Регресія: `LinearRegression()`.
- Виведіть метрики на validation наборі та побудуйте відповідну криву (ROC для класифікації; actual vs predicted scatter для регресії).

> Ці результати стануть вашим **еталоном порівняння** у Частині 6.

---

## Частина 6. Навчання нейронної мережі

### 6.1 Підготовка

- Імпортуйте свою реалізацію `NeuralNetwork` з лабораторних робіт та домашніх завдань.
- Класифікація: закодуйте цільову змінну у one-hot формат для тренування.
- Регресія: переконайтеся, що вихідний шар має 1 нейрон з лінійною активацією, а функція втрат - MSE.
- Визначте кількість вхідних ознак (`n_features`) після пайплайну.

### 6.2 Початкова архітектура

Навчіть модель з такою відправною архітектурою (адаптуйте під свою задачу):

```python
# Classification example
model = NeuralNetwork(
    layer_sizes=[n_features, 64, 32, n_classes],
    activations=['relu', 'relu', 'softmax'],   # or 'sigmoid' for binary
    random_state=42
)
model.fit(X_train, y_train_ohe, lr=0.01, n_epochs=500, verbose=True)
```

### 6.3 Діагностика навчання

- Відобразіть криву втрат (loss curve) для тренувального набору.
- Якщо ваша реалізація підтримує валідаційну втрату - відобразіть обидві криві на одному графіку.
- Чи є ознаки перенавчання або недонавчання?

### 6.4 Вплив регуляризації

Порівняйте три варіанти (змінюйте лише параметри, не архітектуру):

| Варіант | Опис |
|---|---|
| Базовий | без регуляризації |
| L2 | додайте підтримку `l2_lambda` або використайте `weight_decay` |
| Рання зупинка | зупиніть навчання, коли val loss перестає зменшуватися (patience=20) |

- Для кожного варіанту виведіть основну метрику на **validation** наборі.
- Який варіант дає найкращий результат? Чому?

---

## Частина 7. Оцінка моделі

Використовуйте **найкращу модель** з Частини 6 (за метрикою на val).
Усі фінальні метрики звітуйте на **тестовому** наборі.

### 7.1 Основні метрики

**Класифікація** - зведена таблиця для всіх моделей (наївна, лінійна, НМ):

| Модель | Accuracy | Precision | Recall | F1 | ROC-AUC |
|---|---|---|---|---|---|
| Наївна | ... | ... | ... | ... | ... |
| Лінійна модель | ... | ... | ... | ... | ... |
| Нейронна мережа | ... | ... | ... | ... | ... |

**Регресія** - зведена таблиця для всіх моделей:

| Модель | MAE | RMSE | R² |
|---|---|---|---|
| Наївна | ... | ... | ... |
| Лінійна модель | ... | ... | ... |
| Нейронна мережа | ... | ... | ... |

### 7.2 Розширена оцінка

**Класифікація:**
- Побудуйте матрицю помилок (`ConfusionMatrixDisplay`) для нейронної мережі.
- Які класи плутаються найчастіше?

**Регресія:**
- Побудуйте scatter plot: predicted vs actual значення на тестовому наборі.
- Відобразіть розподіл залишків (residuals). Чи є систематична помилка?

### 7.3 Криві якості

**Класифікація:**
- Відобразіть ROC-криві для лінійної моделі та НМ на одному графіку (з AUC у легенді).
- Побудуйте Precision-Recall-криву для НМ. За наявності дисбалансу класів  поясніть, чому PR-крива є більш інформативною (2-3 речення).

**Регресія:**
- Відобразіть криву навчання (learning curve): MAE на train та val залежно від кількості тренувальних зразків.

---

## Частина 8. Оптимізація гіперпараметрів

### 8.1 Sklearn-сумісний wrapper

Використайте власний wrapper:

```python
from sklearn.base import BaseEstimator, ClassifierMixin

class NNClassifier(BaseEstimator, ClassifierMixin):
    def __init__(self, layer_sizes, activations,
                 lr=0.01, n_epochs=500, random_state=42):
        ...
```

### 8.2 Пошук по сітці

Запустіть `RandomizedSearchCV`:
- `n_iter=20`, `cv=3`, `random_state=42`.
- Scoring: `'f1_macro'` для класифікації; `'neg_mean_squared_error'`
  для регресії.

Простір пошуку (адаптуйте `n_out` під свою задачу: кількість класів або 1):

```python
param_distributions = {
    "layer_sizes": [
        [n_features, 32, n_out],
        [n_features, 64, n_out],
        [n_features, 64, 32, n_out],
        [n_features, 128, 64, n_out],
        [n_features, 128, 64, 32, n_out],
    ],
    "activations": [
        # adjust last activation: 'softmax' / 'sigmoid' / 'linear'
        ['relu', 'softmax'],
        ['tanh', 'softmax'],
        ['relu', 'relu', 'softmax'],
        ['tanh', 'tanh', 'softmax'],
        ['relu', 'relu', 'relu', 'softmax'],
    ],
    "lr":       [0.0001, 0.001, 0.005, 0.01],
    "n_epochs": [300, 500, 1000],
}
```

### 8.3 Аналіз результатів

- Виведіть топ-5 конфігурацій за основною метрикою.
- Навчіть кращу знайдену модель на об'єднаному train+val наборі.
- Оцініть на **тестовому** наборі. Чи покращились метрики порівняно з базовою нейронною мережею? На скільки?

---

## Частина 9. Навчання представлень (Representation Learning)

У цій частині ви досліджуєте, що нейронна мережа "навчилась бачити" в даних.

### 9.1 Витягнення прихованих активацій

- Після навчання найкращої моделі витягніть активації **останнього прихованого шару** для всіх зразків тестового набору. (Наприклад, якщо архітектура `[n, 128, 64, k]`, витягніть вихід шару розміром 64.)
- Розмірність матриці активацій: `(n_test, hidden_size)`.

### 9.2 Порівняння просторів

Виконайте t-SNE **двічі** на підвибірці (1 500 зразків) з тестового набору:

1. **До навчання:** на вихідних (нормалізованих) ознаках після пайплайну.
2. **Після навчання:** на активаціях прихованого шару.

Відобразіть обидві проєкції поряд з однаковою схемою розфарбування (за класом або за значенням цілі).

- Чи стали класи (або кластери значень) більш розділимими у вивченому просторі?

### 9.3 Якість представлень (кількісна оцінка)

Навчіть простий `LogisticRegression` / `LinearRegression` (max_iter=1000) на:

1. Вихідних ознаках (тренувальний набір після пайплайну).
2. Активаціях прихованого шару (ті самі тренувальні зразки).

Оцініть обидва варіанти на відповідних тестових представленнях.
Чи дають вивчені представлення кращу лінійну розділимість / регресованість?

---

## Технічні вимоги

| Вимога | Деталі |
|---|---|
| Власні реалізації | Обов'язково використовувати власні PCA, t-SNE та `NeuralNetwork` |
| Sklearn | Дозволено для пайплайну, базових моделей та `RandomizedSearchCV` |
| Відтворюваність | Всі `random_state=42`, зафіксуйте `np.random.seed(42)` на початку |
| Стиль | Чіткі підписи осей, легенди, заголовки графіків (англійською) |
| Markdown-клітинки | Пояснення та висновки - українською мовою |
| Код | Коментарі та змінні - англійською мовою |

---

## Критерії оцінювання

| Розділ | Бали |
|---|---|
| EDA (глибина аналізу, якість візуалізацій) | 15 |
| Preprocessing pipeline (коректність, відсутність витоку даних) | 10 |
| Feature engineering (обґрунтованість, якість відбору) | 10 |
| PCA та t-SNE (власні реалізації, інтерпретація) | 15 |
| Baseline + порівняння моделей | 10 |
| Навчання НМ + діагностика + регуляризація | 15 |
| Оптимізація гіперпараметрів | 10 |
| Representation learning (якісна та кількісна оцінка) | 10 |
| Презентація (захист) | 5 |
| **Разом** | **100** |
